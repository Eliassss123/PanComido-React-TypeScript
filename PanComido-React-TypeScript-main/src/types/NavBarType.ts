export interface NavItemType {
  cadenasVisibles: string;  // lo que se muestra, ej: "Inicio"
  URL: string;   // la URL o ruta, ej: "/home"
  icon?: string;  // opcional, por si queremos agregar íconos después
}
